# Use the CLI

How to select SLOs, supply credentials, and apply (or preview) corrections with the
`datadog-slo-overrides` command.

{%
   include-markdown "../../README.md"
   start="<!-- usage-start -->"
   end="<!-- usage-end -->"
%}
